
SCADA
sourcePool - обработка неработающих источников при старте (зависает)
            восстановление связи отвалившихся источников
достоверность канала!!
SCADA_config - args реализовать использование глобальных констант 
SCADA_config - args реализовать использование переменных произвольных типов с предварительной инициализацией
                например Quie() и тп: инициализаторы через arg:'defult_factory=Quie'
- если result_in массив возможность указания в аргументе элемента массивва по его индексу 
- единый флаг и обработка его сброса для нескольких програм канала

dbQuie  - реализовать возврат данных из select запроса
        - как ждать ответа в цикле пересчета???
            варианты: 
                -каждому запросу свой dbQuie и из результата брать возврат
                -id запроса по нему формировать id ответа и предоставялть в нужнщму обработчику
handlers - имя функции в строчном виде, импорт модуля с функцией из /handlers,
- универсальный шедуллер SHEDULLER=[('Task1',['22:30'], task1)] 
                        def task1(vars):
                        ...
main loop
    startReader               
        - логика при исключении в задаче ?? сейчас вываливается из цикла при ошибке, как вариант - убирать из цикла сбойный канал
HTTP API
    - механизм подписок перевести на DICT-ы 

MB Server
    - addr map hr/ir/di сейчас толлько по типу а не по положению в map HR:{...},IR:{...}

convert_int_AB2BA реализовать


+ разделить globals - переместить параметры каналов в scadaparams.py


